# Dívida técnica rastreada — F0

Fonte: `docs/auditoria/veredito-f0.md` (auditoria de RLS, 4 rodadas, aprovada em 2026-09-04,
176/176 asserts). Este documento existe para que os itens que o veredito listou como "fora de
cobertura" ou "observação com dono" não evaporem depois que F0 fechar — cada linha tem dono e
fica aberta até quem é dono marcar como resolvida (com data e como resolveu, não só riscar).

Regra de manutenção: quem resolve um item edita a linha (status, data, referência ao commit/PR
que resolveu) — não apaga a linha. Item sem dono não entra nesta tabela; volta para o
orquestrador decidir quem assume.

---

## Itens do `devops`

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| D1 | **[Executada em 2026-09-06, no corte C1. Resultado: o GoTrue se comporta como a RLS pressupõe — ver coluna Status.]** **Emissão real de `aal2` pelo GoTrue não validada.** A suíte pgTAP simula o claim `aal` diretamente no JWT de teste — nunca exercitou o fluxo real de enrollment + verificação de TOTP do Supabase Auth para confirmar que o GoTrue só emite `aal2` depois do segundo fator de verdade (e não, por exemplo, logo após o enrollment, antes da primeira verificação). Se o GoTrue emitir `aal2` cedo demais, toda a autorização de `editor`/`conselho` (ADR-0003/0012, que depende de `aal2` no JWT) fica mais fraca do que a suíte local mede. | **Alto.** É a base de toda a autorização de papel privilegiado; um erro aqui não aparece em nenhum teste atual. | **FECHADA no stack local — 2026-09-06, corte C1.** Sonda executável em `scripts/probe/aal2-gotrue.ts`, 10/10 verificações verdes contra o GoTrue de verdade: sessão nasce `aal1`; **enrolar o TOTP não promove nada** — nem o token existente, nem um token renovado depois do enrolamento; fator recém-enrolado fica `unverified`; código errado é recusado e não devolve token; só o código certo promove para `aal2`; e **login novo de usuário com fator já verificado volta a `aal1`**, ou seja, `aal2` é propriedade da sessão, não da conta. É exatamente o que a RLS pressupõe (ADR-0003, ADR-0012) — os 202 asserts de pgTAP medem uma garantia que existe. Exigiu ligar `[auth.mfa.totp]` em `supabase/config.toml`, que vinha desligado. **O que continua aberto é D2, não D1:** isto foi medido no GoTrue local; o hospedado não foi exercitado. |
| D2 | **Comportamento em Supabase hospedado não confirmado.** Toda a suíte (176 asserts) rodou contra o stack local (`supabase start`, Postgres em container Docker). O bloqueio de `TRUNCATE` para `service_role` foi confirmado como propriedade do Postgres em si (grants), então vale nos dois ambientes. O resto — timing de `aal2` (D1), comportamento de `pg_net`/extensões geridas, papéis/roles internos do Supabase hospedado — não foi exercitado contra um projeto real. | **Médio-alto.** Diferença de ambiente hospedado é justamente o tipo de coisa que só aparece em produção se não for testada antes. | **Aberto.** Bloqueado por decisão pendente de provisionar um projeto Supabase remoto de staging (fora do escopo/orçamento deste agente decidir sozinho — ver `docs/ops/backup.md` §3 e `.claude/agents/devops.md`, "Quando escalar": mudança/provisionamento de provedor é decisão conjunta `arquiteto` + dona do projeto). |
| D3 | **`pg_prove` — mecanismo de execução no CI, não só localmente.** Nesta rodada de fechamento: `pg_prove` foi instalado nesta máquina (`cpanm TAP::Parser::SourceHandler::pgTAP`) e `.github/workflows/ci.yml` (job `rls-test`) passou a instalá-lo de verdade no runner (apt `libtap-parser-sourcehandler-pgtap-perl`, com fallback `cpanm`) em vez de depender do fallback interno do Supabase CLI. Rodei a suíte real 3 vezes nesta máquina (2 sobre o mesmo stack, 1 após `supabase db reset --local` completo) — **176/176, determinístico, exit code 0** nas três, igual ao veredito do `auditor-rls`. | **Baixo, residual.** A mecânica de execução local está confirmada; o que não foi confirmado é o runner real do GitHub Actions (imagem `ubuntu-latest` hospedada), só simulado aqui via Docker Desktop no macOS. | **FECHADA — 2026-09-08; reconfirmada em 2026-09-11 no run `34619369719`** — o primeiro do repositório público, recriado do zero com o histórico redigido (os runs e SHAs de antes morreram com o repositório privado apagado, então nenhum identificador anterior é citado aqui). Primeira execução real do workflow num runner hospedado do GitHub (`ubuntu-latest`): os 9 arquivos da suíte passaram, **320 testes, `All tests successful`, `Result: PASS`**, com `pg_prove` vindo da imagem `ghcr.io/supabase/pg_prove:3.36` puxada pelo próprio CLI no runner. A cadeia inteira (lint → typecheck → unit → a11y → rls) ficou verde e o `ci-gate` passou por mérito, não por `skipped`. **O que a primeira tentativa revelou, e que a suspeita original não previa:** o workflow nunca havia rodado — `pnpm/action-setup@v4` recusava duas fontes de versão (`PNPM_VERSION` no workflow + `packageManager` no `package.json`), o job de lint morria antes de instalar qualquer dependência e os quatro seguintes ficavam `skipped`. Ou seja: entre o primeiro push e 2026-09-08, **nenhum** assert de RLS rodou no CI. Corrigido junto: Node do CI alinhado com o de desenvolvimento (20 → 24) e as variáveis do Supabase no job de a11y — sem elas o `proxy.ts` derruba `/design-system` em 500, defeito que só apareceu quando o job passou a poder rodar. |
| D19 | **A produção na Vercel existe e não bate com o que os documentos descrevem.** Verificado em 2026-09-11: (1) `breeze-alpha-one.vercel.app` responde a qualquer um (`/` → 307 → `/entrar`, 200) — **sem Deployment Protection**, com deploy já feito, e o item A6 do Portão A pedia a proteção *antes* do primeiro deploy; (2) o projeto Vercel **não tem Git conectado** — os 5 deploys de produção saíram da CLI, nenhum push gerou deploy — então a restrição `git.deploymentEnabled` a `main` em `vercel.json`, em que o item A5 se apoia, **não protege nada**: deploy de produção hoje é manual, de qualquer máquina e de qualquer branch; (3) Production tem `NEXT_PUBLIC_SUPABASE_URL`, `ANON_KEY`, `SERVICE_ROLE_KEY`, `DB_URL` e `CPF_HASH_PEPPER` desde 2026-09-07, marcadas *sensitive* (a CLI não devolve o valor), e a conta Supabase conectada ao agente não tem projeto Breeze. **Inferência, não verificado:** os valores foram copiados do `.env.local` e apontam para `127.0.0.1` — o que manteria D2 correta. | **Médio.** Não há dado real atrás (o acervo nunca subiu), então nada vaza hoje; o risco é o Portão A ser executado sobre premissas falsas — duas travas que o parecer dá como existentes (A5, A6) não existem. | **Aberto.** Dona: conferir no painel da Vercel o valor de `NEXT_PUBLIC_SUPABASE_URL`; se for `127.0.0.1`, apagar as cinco variáveis. `devops`: ligar Deployment Protection agora (não depende de nada) e decidir entre conectar o Git à Vercel ou documentar o deploy manual no runbook. |

---

## Itens do `eng-supabase`

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| E1 | **Storage API real não coberta pela suíte.** A suíte pgTAP testou a RLS de `storage.objects`; signed URL, TTL curto (60–300s, SPEC §7) e a checagem de papel no servidor que emite a URL são código de aplicação, fora do alcance de pgTAP. | **Alto** — é exatamente o mecanismo que protege anexo financeiro (SPEC §7, "nunca embutida em página cacheada na CDN"). | Aberto — dívida de F1 (upload/leitura de documento). **Agendado em 2026-09-06:** o upload por signed URL entra no corte **C2** e o download com TTL curto no **C6** (leitor) de `docs/f1-plano.md`; a cobertura de teste é E2E, não pgTAP, e precisa do papel de aplicação que o time ainda não tem (ver ADR-0029, "F1 exige um papel que o time não tem"). Anexo financeiro em si é F2 — esta linha só fecha quando a rota que assina for testada com papel errado e o teste **negar**. |
| E2 | **Documento pode sumir da busca em silêncio.** O invalidador de chunks aposta em reprocessamento idempotente do worker, mas nada no schema enfileira esse reprocessamento — nenhuma função referencia `job.fila`. Se o worker não rodar, o documento some da busca sem erro, sem alerta, sem log. | Médio — falha silenciosa, sem sinal para o operador. **Reavaliado para alto em 2026-09-06:** é o caminho obrigatório da ata AGE 04.02.2026 (o documento que motivou o modelo de visibilidade por página), então não é hipótese — é o fluxo. | **Mecanismo de banco IMPLEMENTADO e provado — 2026-09-06, corte C4, `eng-supabase`.** Migração `supabase/migrations/20260906140000_reprocessamento_enfileirado_e_sentinela.sql`: `documentos.indexado_em` (eixo separado do `status`); três triggers genéricos em `chunks`/`documentos` que zeram/confirmam `indexado_em` e chamam `job.enfileirar()` na mesma transação sempre que um chunk sai ou entra, ou `versao_pipeline` muda; `documento_paginas_invalida_chunks_afetados` (baseline 08) estendida para reagir também a `DELETE` (caminho "apagar página", que não era coberto). View sentinela `app.documentos_fora_da_busca`, gestão-only. **INV-13 preenchida** (`docs/invariantes/INV-13-derivado-invalidado-implica-reconstrucao-enfileirada.md`), matriz de 19 linhas + 4 fixas, nível 3 (invalidação/reprocessamento). **Provado:** `supabase/tests/09_reprocessamento_enfileirado_sentinela.sql` (34 asserts, os 4 caminhos vermelhos do ADR — reclassificar página, apagar página, apagar chunk à mão, mudar `versao_pipeline` — cada um resultando em job pendente E documento na sentinela; bloco E prova que a chave de reenfileiramento funciona de novo DEPOIS que o job anterior concluiu, o caso que a unicidade total do E2 original quebrava); suíte inteira 264→298, sem regressão. Prova end-to-end real (ingerir o Regimento, reclassificar uma página, `pnpm worker:uma-vez`): chunks invalidados e recriados, `indexado_em` zera e volta, `status` permanece `publicado` durante toda a invalidação. **Achado novo durante a prova, FORA do escopo deste agente (schema apenas) — E2 não fecha de ponta a ponta sem ele:** `lib/ingestao/estagios.ts`, função `chunking()` (estágio 4), escreve `status = 'em_revisao'` INCONDICIONALMENTE ao final, inclusive ao REPROCESSAR um documento já `publicado` — na prova real, depois do worker recriar os chunks corretamente, o documento saiu de `publicado` e foi para `em_revisao`, o que o derruba da RLS do morador (exige `status='publicado'`) e reproduz, por outra porta, exatamente o estrago que o ADR-0026 §3 explicitamente veta ("rebaixar status faz o documento sumir inteiro"). Por ADR-0025 §1 (tabela de estágios), o estágio 4 (`chunking`) não deveria escrever `status` nenhum — só o estágio 6 (`classificação`, desligado em F1 pelo ADR-0027) o faz; a linha atual parece um atalho para sinalizar "pronto para curadoria" no fluxo de ingestão INICIAL que não foi guardado contra o fluxo de REPROCESSAMENTO. Correção sugerida (não aplicada por este agente): condicionar a escrita a `where id = $1 and status <> 'publicado'`. **Dono da correção: quem mantém `lib/ingestao/estagios.ts`/worker (F1, cortes C2/C3 do ADR-0025)** — registrado aqui como nova linha de dívida em vez de consertado in-place, por disciplina de escopo por arquivo (dois agentes não escrevem no mesmo lugar). `devops` segue dono da sentinela no log do worker e no runbook (não tocado nesta entrega). |

## Itens do `arquiteto`

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| A1 | **`deliberacoes.trecho_literal` é snapshot protegido por âncora móvel.** Mover `(documento_id, pagina)` para uma página pública torna o trecho legível. Local (a âncora está na mesma linha) e exige papel `editor` (que já pode publicar o que quiser — risco D4 já registrado), mas a semântica é frágil. | Baixo-médio, mitigado por já exigir papel privilegiado. | Aberto — observação do veredito, item 2. |
| A2 | **`documento_paginas` é tabela de autorização e não é auditada.** | Médio — tabela que decide visibilidade não deixa trilha de quem mudou o quê. | **Decidido, não implementado — 2026-09-06, `ADR-0028` §5.** Auditar em `audit.log` **em F1**, junto com `documento_paginas.motivo_override` obrigatório quando há override — a justificativa de por que uma página de documento autenticado virou pública é o que uma auditoria vai querer ler, e o override contorna `tipos_documento.permite_publico` por desenho. Motivo do prazo: F1 é quando a tabela começa a ser escrita de verdade (curadoria dos 43); auditar depois de já haver histórico é pior e mais caro. **Dono:** `eng-supabase`. Corte **C8** (ou antes, junto de C5, se a curadoria começar a marcar override). |
| A3 | **`unidades` não é auditada**, e `fracao_ideal` define rateio. | Médio — mudança de fração ideal sem trilha afeta cálculo financeiro de todos os condôminos. | Aberto — observação do veredito, item 4. |
| A5 | **Worker usa `service_role`, e o ADR-0007 pedia credencial própria.** A baseline de F0 concedeu os `GRANT`s do pipeline ao `service_role` (achado V4), e é esse conjunto que foi auditado nas 4 rodadas. Criar um papel `breeze_worker` agora duplicaria e obrigaria a re-auditar a matriz de privilégios para separar duas chaves que ficam **na mesma máquina, com a mesma pessoa** — separação no papel, não no fato. | Baixo hoje, **médio no dia em que o worker sair da máquina da mantenedora**: aí `service_role` num host remoto é uma chave de bypass total de RLS fora do controle físico dela. | **Aceito conscientemente com gatilho — 2026-09-06, `ADR-0025` §7.** O papel dedicado entra **no mesmo dia** em que o worker rodar em qualquer lugar que não seja a máquina da mantenedora. Não é pendência sem data: é decisão com condição de disparo. **Dono:** `arquiteto` (decide) + `eng-supabase` (migração de `GRANT`s) + `devops` (rotação). |
| A4 | **Assinatura de parecer × anonimização de ex-morador.** `parecer_signatarios` guarda só `pessoa_id`; o nome do signatário vive só em `pessoas.nome`. Anonimizar um ex-morador que assinou parecer apaga o nome do signatário de um documento já emitido. Deliberadamente sem teste vermelho — decisão de produto/jurídica antes de travar comportamento. Dono conjunto `arquiteto` + `juridico-lgpd`. | Médio — tensão real entre LGPD (apagar PII) e integridade de documento histórico assinado. | Aberto — observação do veredito, item 5; precisa de decisão, não só de código. |

## Itens do `auditor-rls`

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| R1 | **Corridas de concorrência fora do CI.** Não são expressáveis em pgTAP de transação única (`begin`/`rollback` por arquivo); verificadas por script de conexões paralelas rodado manualmente, fora do pipeline. | Médio — regressão de corrida (ex.: os cenários "resta 1" do V10) não é pega automaticamente. | Aberto — mecanismo de verificação manual, não automatizado. |
| R3 | **[Julgada e corrigida em 2026-09-06, no corte C2. O achado original continua descrito abaixo; o que mudou está na coluna Status.]** **Assert da suíte compara a tabela inteira, não a fixture.** Achado em 2026-09-06, no corte C1: o assert 26 de `02_papeis_aal_mandato_pessoas_cobrancas_rls.sql` ("C10 conselho vê CPF mascarado") lista **todos** os CPFs mascarados de `pessoas` e compara com quatro valores fixos. Quatro moradoras criadas pelo teste de ponta a ponta (`tests/e2e/entrar.spec.ts`) fizeram a auditoria de segurança falhar sem que nada da segurança tivesse mudado. Já mitigado do lado de quem sujou — o e2e e a sonda D1 agora limpam o que criam — mas **a fragilidade é do assert**: ele mede o estado do banco em vez do efeito da policy. | Médio — falso vermelho na suíte de segurança gasta confiança justamente onde a confiança é o produto. Falso **verde** é o risco espelhado: um assert que depende do banco estar vazio pode estar passando por sorte. | **FECHADA — 2026-09-06, corte C2, pelo `auditor-rls`.** Varredura completa de `supabase/tests/` (7 arquivos): a classe tinha **5 instâncias**, não as 2 relatadas. (1) `01/A1` agregava `documentos` inteira contra um título — a primeira convenção publicada pelo backfill quebrava a auditoria. (2) `02/C10` (o achado original). (3) `02/D5` contava `3` cobranças fixas como "controle de que a gestão vê tudo". (4) `02` bloco F: `update papeis set mandato_fim = current_date - 1 where papel='editor' and pessoa_id <> solo` — escrita global **sem escopo** que, com a editora de `scripts/dev/semeia-editora.ts` (`mandato_inicio = current_date`), viola `papeis_mandato_ck` e **aborta o arquivo inteiro** (10 asserts nunca rodaram, `Bad plan`). (5) `03/E3` endereçava a linha a adulterar por `max(seq)-1` — exatamente o `seq - 1` que o SPEC §7 proíbe ("seq dá ordem, não endereço") e que o próprio `audit.verificar_cadeia` já tinha sido corrigido para não fazer (V5-R). **Correção, sem relaxar nada:** cada assert global virou *dois* — um escopado à fixture (o valor concreto) e um de **containment em forma de policy**, que é global de propósito e fica **mais forte** quanto mais povoado o banco (`A1b`: nenhuma linha que o anônimo alcança em `documentos` foge de publicado+público; `C10b`: nenhuma linha que o conselho alcança pela view sai fora da máscara; `D5`: igualdade com o total real da tabela, não com `3`). A pré-condição global do bloco F ("a Solo é a única editora vigente") passou a ser **construída de forma legal e CONFERIDA** por um assert novo (`F0`) em vez de suposta. `03/D8` e `05/A4` ganharam marca d'água `_base` em `audit.log`. **Prova:** os 6 arquivos ganharam bloco `RUIDO: banco POVOADO` que suja o banco dentro da própria transação (editora com mandato aberto hoje, moradoras com CPF, acervo publicado, cobranças) — o CI passa a provar a imunidade a cada rodada, mesmo em runner com banco limpo. Medido nos dois estados: **banco vazio 234/234 PASS**; **banco povoado (editora semeada + 2 moradoras + 3 documentos + 3 chunks + 2 cobranças + 4 objetos) 234/234 PASS**, contra **218/FAIL** da suíte de antes no mesmo banco. Não relaxado: 4 mutações de RLS injetadas de propósito deixaram a suíte vermelha (policy permissiva para `anon` → `A1`+`A1b`; view vazando CPF só fora da fixture → `C10b` pega, `C10` não; policy restritiva em `cobrancas` → `D5`; fixture sem a pré-condição → `F0` mais 7 asserts do bloco F). |
| R2 | **`deliberacoes` sem assert na suíte.** Verificado manualmente nas 3ª e 4ª rodadas; não codificado como teste. | Médio — mesma classe de risco de R1: verificação existe, mas não é regressiva. | Aberto. |
| R4 | **Não existe revogação no mesmo dia de um mandato de `editor` — e isso é do SCHEMA, não do teste.** Descoberto em 2026-09-06 ao consertar R3. `papeis.mandato_inicio`/`mandato_fim` são `date` e a vigência é `mandato_fim >= current_date`, mas a autorização que o mandato concede é instantânea. Consequência: para um mandato aberto **hoje** (o caso normal — `scripts/dev/semeia-editora.ts`, e a cerimônia manual equivalente em produção), **nenhum caminho legal o encerra hoje**: `mandato_fim = current_date - 1` viola `papeis_mandato_ck` (`ERRO[23514]`, medido); `mandato_fim = current_date` é aceito mas a pessoa segue vigente o dia inteiro (`eh_editor_vigente_linha` = `true`, medido); `delete` é negado a todos de propósito (B6/B7); reescrever `mandato_inicio` falsifica o registro de quem foi editora e quando. Cenário banal: erro de cadastro, desistência, troca de editora no mesmo dia. | **Médio-alto.** Com `editor` única (D4) e `aal2`, quem entrou por engano fica com **toda a escrita do sistema até amanhã**, e a única saída é acesso direto ao banco. É o oposto do "o acesso cessa em `vinculos.fim` + 0 dias" que o SPEC §7 registra como veto do `juridico-lgpd`, e do princípio de "nunca monotonizar predicado constitutivo" da mesma seção. O mesmo raciocínio de granularidade vale para `vinculos.fim` (ex-morador vendeu hoje). | **DECIDIDO, não implementado — 2026-09-06, `ADR-0030` (`arquiteto`).** Decisão: **vigência é intervalo meia-aberto de instantes, `[inicio, fim)`, avaliado em `now()`**. As quatro colunas de período (`papeis.mandato_inicio/mandato_fim`, `vinculos.inicio/fim`) passam de `date` para `timestamptz`, e o predicado passa de `fim >= current_date` (fechado, dia) para `fim > now()` (meia-aberto, instante) — com isso `fim = now()` é aceito **e** efetivo no mesmo instante, que é a operação que hoje não existe. `vinculos.fim` entra na **mesma** migração, não depois: são as duas pernas de `app.eh_autenticado()`, e moradores são muitos enquanto a editora é uma. `papeis` ganha `encerrado_por` + `motivo_fim` (enum, nunca texto livre — texto livre sairia `[REDIGIDO]` em `audit.log`). **Premissa do pedido corrigida no ADR §6:** revogar e conceder **não** precisam ser atômicos para a INV-02, que é um piso (`>= 1`) e não uma igualdade — conceder antes de revogar faz a contagem ir `1 → 2 → 1` sem passar por zero, e o trigger do ADR-0022 já força essa ordem. Atomicidade fica como higiene da D4 (evitar duas editoras em silêncio se a segunda etapa falhar), com ordem obrigatória `INSERT` da nova → `UPDATE` da antiga na mesma transação. **Não exigiu consulta ao `juridico-lgpd` para migrar** — implementa o "+ 0 dias" em vez de reinterpretá-lo. A única pergunta aberta (o *default da tela* para saída planejada) foi **respondida em 2026-09-06**, parecer `docs/juridico/pareceres/2026-09-06-default-da-tela-encerramento-de-vinculo.md`: "encerra no dia D" vale **até o fim do dia D** (CC art. 1.245 — a propriedade transfere-se com o registro do título, fato com data e sem hora conhecida do condomínio). O mesmo parecer **recusou a forma do §3** e o ADR foi emendado: a ponta final **deriva de `motivo_fim`**, não é escolha da editora — motivo datado (`venda`, `fim_locacao`, `termino_de_mandato`) → `(D+1) 00:00 America/Sao_Paulo`; motivo instantâneo (`obito`, `pedido_titular`, `erro_cadastral`, `renuncia`, `substituicao`, `conta_comprometida`) → `greatest(inicio, now())`. **Nada mudou no banco** (tipo, predicado, enum, constraints e os 7 passos seguem válidos); a emenda é toda de tela. Motivo da recusa, que era achado real: a tabela binária contradizia o §6 do próprio ADR — registrar entrega de gestão como "encerra hoje" deixaria **duas editoras vigentes até a meia-noite**, reabrindo pela tela a janela que a migração fecha no banco. **Especificação de implementação pronta e fechada** no ADR: 7 passos, uma migração só (estado meio-aplicado estende todo mandato em um dia, em silêncio, na direção permissiva), com as duas armadilhas nomeadas — `using (mandato_fim + 1)::timestamptz`, nunca cast direto (cast direto revoga um dia retroativamente para todo mundo), e `drop function` explícito de `eh_editor_vigente_linha`, porque `create or replace` com tipos diferentes cria **sobrecarga** e deixa a versão `date` viva sob os dois triggers. **Dono da migração: `eng-supabase`.** **Dono dos testes: `auditor-rls`** — os dois `todo` do bloco G viram verde-inesperado no dia do conserto, `G2` precisa passar `now()` no lugar de `current_date`, o `least(mandato_inicio, current_date - 60)` do bloco F deixa de ser necessário (a operação real passa a ser legal), e falta o assert que hoje não existe: revogar com `fim = now()` e afirmar `tem_papel` = `false` na mesma transação. Invariantes afetadas: **INV-02** (enunciado e F4/fuso, que deixa de ser `ACEITO`), **INV-06**, **INV-12**, e **INV-14 nova**. SPEC §2/§2.1 têm correção **proposta e não aplicada** (fora do escopo do `arquiteto` sem aprovação do orquestrador). **FECHADA em 2026-09-06** — ADR-0030 (vigência é intervalo meia-aberto de instantes), migração `20260906160000_vigencia_em_instante.sql`, auditoria aprovada em `docs/auditoria/veredito-20260906160000-vigencia-em-instante.md` (320/320, zero `todo`). Verificado pelo orquestrador contra o banco: mandato concedido às 01:41:56 e revogado às 01:42:00 com `fim = now()` deixa de ser reconhecido **no mesmo instante**, com a trilha preservada (`motivo_fim`, `encerrado_por`) e a editora legítima vigente o tempo todo. |
| R5 | **Resíduos da varredura de R3 que ficaram abertos por escolha, não por esquecimento.** (a) As fixtures reservam UUIDs fixos (`00000000-`, `20000000-`, `30000000-`, `ff…`); se uma linha real nascer com um deles, o arquivo aborta com `duplicate key` — aconteceu comigo durante a auditoria. (b) `01/C5`,`01/C6`,`01/F2`,`01/F6`,`03/E1`,`03/E2`,`05/B5` continuam **globais de propósito**: são invariantes sobre tudo que existe (espelho chunk↔página, uniformidade de nível, integridade da cadeia de hash) e ficam mais fortes com o banco cheio — mas se um documento real do backfill violá-las, o vermelho é achado de verdade, não fragilidade de teste, e não deve ser "consertado" escopando. (c) `03/D8` e `05/A4` são robustos a povoamento mas não a **concorrência**: outra sessão escrevendo em `audit.log` durante a rodada muda "a última linha" — mesma classe de R1. | Baixo. (a) falha alto e claro, nunca em silêncio, e nunca vira falso verde. (b) é desenho. (c) é R1 por outra porta. | **Aberto, consciente.** Dono: `auditor-rls`. (b) não deve ser mexido; (a) e (c) só viram problema se o CI ganhar paralelismo ou se algum script de seed passar a usar UUID fixo. |


## Itens do `juridico-lgpd`

Abertos pelo parecer `docs/juridico/pareceres/2026-09-07-producao-dado-real-supabase-vercel.md`
(subida a produção com o acervo real). Os itens **bloqueantes** não estão nesta tabela — estão no
checklist de portões do próprio parecer, porque bloqueiam e não são dívida. Aqui ficam só os que
foram conscientemente adiados, com dono.

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| J1 | **Lacuna de cláusulas-padrão contratuais da ANPD (Resolução CD/ANPD nº 19/2024).** Verificado no texto dos dois DPAs: nem Supabase nem Vercel mencionam LGPD, Brasil ou ANPD, e nenhum incorpora as cláusulas-padrão brasileiras — ambos incorporam SCC europeias. O período de adequação encerrou em 23/08/2025. Não é fechável pela dona do projeto: condomínio de ~50 unidades não negocia adendo com Vercel Inc. | **Baixo-médio.** Risco residual de conformidade formal sobre tratamento de baixo risco, com o corpo do dado em `sa-east-1`. Não vira incidente sozinho; vira exposição se houver questionamento. | **Aberto por escolha, assumido.** Dono: dona do projeto. Mitigação em vigor: região São Paulo + direção expressa de região + registro no ROPA como risco assumido. **Reavaliar em 2027-09** ou quando qualquer dos dois publicar termos brasileiros. |
| J2 | **Redação de CPF/RG no texto extraído não existe no pipeline.** A ata da AGI de 04.12.2025 traz CPF e RG completos, em claro, do síndico. Sem uma etapa de redação antes do chunking, indexar essa ata torna o CPF recuperável por busca — e o chunk vira `pessoas` sem os controles de `pessoas`. | **Alto no Portão C, nulo antes dele.** Enquanto tudo está `em_revisao`, só a gestão lê. Vira alto no instante em que a ata for publicada. | **Aberto. Bloqueia publicação (Portão C, item C1).** Dono: quem mantém `lib/ingestao/extracao.ts`. |
| J3 | **Qualificação das partes na Convenção registrada não foi inspecionada.** Convenção registrada em cartório costuma trazer nome, CPF, RG e endereço do representante da incorporadora e dos proprietários originais. É o único documento cuja visibilidade padrão é `publico` (bucket `publicos`, leitura sem login). | **Alto se publicada sem olhar** — exposição de PII de terceiro à internet aberta, irreversível na prática. | **Aberto. Bloqueia a ida ao bucket `publicos` (Portão C, item C2).** Dono: `editor`, na conferência de OCR. |
| J4 | **Retenção de log do provedor (Supabase/Vercel) não definida.** Ambos guardam logs de requisição por período próprio, que podem conter caminhos com identificadores. Ninguém decidiu o que fazer. | **Baixo.** O ADR-0004 regra 5 já proíbe nome informativo em objeto de Storage, então o caminho carrega UUID, não conteúdo. | **Aberto.** Dono: `devops`. |
| J5 | **Titularidade da conta ≠ titularidade do dado.** Conta Supabase/Vercel é pessoal da dona; o controlador é o condomínio. Enquanto não houver registro escrito da adoção do Breeze pelo condomínio, a leitura de fato é que a dona é controladora pessoalmente (LGPD art. 42). | **Alto a partir do Portão B**, nulo antes: com um só usuário, o tratamento cabe no legítimo interesse próprio dela como condômina e conselheira. | **Aberto. Bloqueia o cadastro da segunda pessoa (Portão B, item B1).** Dono: dona do projeto. Caminho mais curto e formas aceitáveis no §6.3 do parecer. |
| J6 | **Migração da conta para titularidade do condomínio.** Mesmo com o controlador registrado, a contraparte contratual de Supabase/Vercel segue sendo pessoa natural. Se a dona sair do conselho, o condomínio não tem título sobre a conta onde está o próprio acervo. | **Médio, e cresce com o tempo.** Interage com o bus factor = 1 do Risco §8.5. | **Aberto, sem prazo.** Dono: dona do projeto. Mitigação em vigor: backup em conta separada + chaves em cofre externo + declaração de custódia. |
| J7 | **Aviso de privacidade, canal do titular e procedimentos de titular/incidente não existem.** Busca por "privacidade/titular/encarregado/LGPD" em `app/`, `components/` e `lib/` retorna uma única ocorrência, num comentário. | **Alto a partir do Portão B** (art. 9º, art. 18 §1º, art. 48), nulo antes. | **Aberto. Bloqueia o Portão B (itens B2, B3).** Donos: UI + `devops` (runbook). Prazo do art. 48 a confirmar no texto da Resolução CD/ANPD nº 15/2024 antes de escrever número no runbook. |
| J8 | **O condomínio trata biometria por fora do Breeze.** Terminais de reconhecimento facial para áreas comuns (deliberado na AGE de 30.04.2026) e cadastro facial por app de terceiro. Biometria é dado sensível (art. 11, II, "b"), com regime muito mais pesado. | **Não é risco do Breeze** — é exposição do controlador que ninguém mapeou. Vira risco do Breeze no dia em que alguém propuser integração com o controle de acesso. | **Escalado à dona do projeto, fora do escopo do produto.** Qualquer proposta de integrar o Breeze ao controle de acesso exige parecer novo; a resposta padrão é não. |

---

## Itens novos de F1 — corte C1

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| C1-1 | **O teste de ponta a ponta de autenticação não roda no CI.** `tests/e2e/entrar.spec.ts` (4 casos: indistinguibilidade da resposta de login, magic link virando sessão, TOTP promovendo para `aal2`, rota privada redirecionando) exige `supabase start` — Docker, GoTrue e Mailpit de pé. O job de a11y do CI não tem stack. Hoje ele roda por `pnpm test:e2e`, à mão. | **Alto.** É a única cobertura automatizada do fluxo de entrada, e cobre a propriedade de segurança que a tela de login precisa manter (não virar oráculo de "esta pessoa mora aqui"). Fora do CI, ela regride sem ninguém ver. | **Aberto.** Dono: `devops`. Precisa de um job com `supabase start` — o mesmo que o `rls-test` já faz. |
| C1-2 | **`cpf_enc` não é escrito por código nenhum ainda.** `lib/identidade/cpf.ts` implementa o HMAC de lookup (`cpf_hash`), mas a cifra AES-256-GCM do ADR-0014 não existe em código — o teste e2e preenche `cpf_enc` com bytes de enfeite só para satisfazer o check de par. | Médio — só vira problema quando a editora cadastrar pessoa de verdade, que é outro corte. Registrado para não descobrir isso na hora do cadastro. | **Aberto.** Dono: quem implementar o cadastro de pessoa pela editora. |
| C1-3 | **Códigos de recuperação (D9) continuam fora do produto, por decisão — e agora há uma tela que depende disso.** `/seguranca` instrui a gerar e imprimir os códigos (runbook §5.1), mas nada no sistema verifica que a cerimônia aconteceu. | Médio-alto para a editora única: perder celular e papel ao mesmo tempo é perda total do acesso de escrita. | **Aberto por desenho.** O runbook §5.5 prevê teste trimestral; a primeira execução real ainda não aconteceu. |

---

## Itens novos de F1 — corte C2

| # | Item | Risco se não resolvido | Status |
|---|---|---|---|
| C2-1 | **[Achado e corrigido em 2026-09-06, no corte C2.]** `INSERT ... RETURNING` e `UPDATE ... RETURNING` em `public.documentos` eram recusados com `42501` para QUALQUER papel, inclusive editor com `aal2`. Causa: `documentos_select` chamava `app.documento_visivel(id)`, que reconsulta `public.documentos` — e sob MVCC uma subconsulta disparada pelo mesmo comando não enxerga a própria linha que esse comando está inserindo/alterando (cid da linha nova == cid do comando corrente). É a espécie MODAL do ADR-0023 ("predicado de autorização deve ser local"): a releitura não protegia nada, só quebrava RETURNING. | **Foi alto** — travava o caminho normal de entrada do acervo (Server Action de upload e script de backfill, SPEC §3), embora os dois já contornassem o defeito pré-gerando o `id` no cliente e nunca pedindo `RETURNING` (comentários em `app/acervo/enviar/acoes.ts` e `scripts/backfill/enviar.ts` documentam o contorno — hoje **desatualizados**, já que a causa-raiz foi corrigida; ficam como dívida de limpeza de comentário, sem efeito funcional, dono de quem tocar esses arquivos depois). | **Fechado — 2026-09-06, `eng-supabase`.** Migração `supabase/migrations/20260906100500_documentos_select_predicado_local.sql`: `documentos_select` passa a avaliar `app.eh_gestao() or (status = 'publicado' and app.nivel_visivel(visibilidade, id))` inline, lendo só a própria linha — sem reconsultar `documentos`. `app.documento_visivel(uuid)` permanece intacta para quem pergunta de FORA da tabela (`storage.objects`). Precisou de um `grant execute` novo em `app.nivel_visivel` para `anon` (a chamada deixou de estar embrulhada numa função `security definer` que dispensava o grant direto). Varredura da mesma classe feita sobre `documento_paginas`, `chunks`, `pessoas`, `papeis`, `cobrancas`, `lancamentos`, `assembleias`, `deliberacoes`, `contas`, `fornecedores`, `contratos`, `periodos_fechados`, `orcamento`, `unidades`, `vinculos`, `documento_unidades`, `tipos_documento`, `lancamento_anexos` — nenhuma outra tem o defeito (`documento_paginas`/`chunks` só são escritas por `service_role`, que ignora RLS por completo; `pessoas`/`papeis` reconsultam a própria tabela dentro de uma função no segundo operando de um `or`, mas o primeiro operando já é local e cobre exatamente o caso em que a escrita afeta a própria linha da sessão). Prova: suíte pgTAP **234/234 antes e depois** (2 TODO conhecidos de R4, inalterados); `pnpm db:types`, `pnpm lint`, `pnpm typecheck`, `pnpm vitest run` (80/80) verdes; `INSERT`/`UPDATE ... RETURNING` verificados manualmente contra o stack local para editor `aal2` (funciona), editor `aal1`, conselho e morador (negado nos quatro casos) — verificação ad hoc, não commitada como teste pgTAP (fora do escopo declarado de `eng-supabase`; recomendado a `auditor-rls` formalizar os 8 casos citados como asserts regressivos). |

---

## Verificação de fechamento (item cobrado à parte pelo orquestrador)

O gate de CI (`ci-gate` em `.github/workflows/ci.yml`) precisa continuar rodando a suíte de RLS
em **toda** alteração de migração e barrar o merge de verdade — não só "rodar e reportar". Ver
`README.md` (seção CI/CD) para o desenho do gate. **Branch protection ligada em 2026-09-11**, com
`ci-gate` como único required status check — barra merge de PR vermelho; push direto da
administradora segue possível (`enforce_admins` desligado, ver README). Esta linha é a mais importante da tabela: as quatro rodadas de
auditoria só valem para sempre se a regressão for barrada automaticamente daqui para frente.
