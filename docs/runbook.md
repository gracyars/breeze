# Runbook — ponteiro

O conteúdo operacional real está em `docs/ops/`, conforme o escopo declarado do agente `devops`
(`.claude/agents/devops.md`):

- **Backup, retenção e teto de custo:** `docs/ops/backup.md`
- **Restauração passo a passo + registro de testes trimestrais:** `docs/ops/runbook-restauracao.md`

Este arquivo existe só para quem procurar por `docs/runbook.md` diretamente; não duplicar
conteúdo aqui — editar sempre os arquivos em `docs/ops/`.
