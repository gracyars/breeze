# lib/

Código compartilhado sem UI: clientes Supabase (`lib/supabase/`), helpers de domínio, tipos
gerados (`lib/supabase/database.types.ts`, gerado no CI — nunca editado a mão).

Sem lógica de negócio ainda — escopo de `eng-supabase` e dos agentes de feature (F1+).
